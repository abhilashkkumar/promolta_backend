# promolta_backend
dependency:
	ruby
	rails
	bundler

commands to run: 
1. 'bundle install'
2. change mysql root password in config/database.yml
3. 'rake db:create'
5. 'rails s -p 4000'
6. use mysqldump by mysql
don't forget to migrate the mysql data
